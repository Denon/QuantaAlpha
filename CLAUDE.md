# Project conventions

## Python execution
All Python scripts and commands must use the `rdagent` conda environment. Use `conda run -n rdagent python ...` to execute Python code.

## Factor library
- Stored at `data/factorlib/all_factors_library.json`
- Backfill script at `scripts/backfill_factor_metrics.py`
